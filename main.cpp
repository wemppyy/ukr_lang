#include "ukr_lang.h"
#include <Windows.h>
#include <iostream>


#define сума sum

використати простір_імен стандартний;

ціле сума(ціле a, ціле b) {
	повернути a + b;
}

головна {
підключити_українську();
	ціле a дорівнює 5;
	ціле b дорівнює 10;
	ціле c дорівнює сума(a, b);
	вивести вивести_у_потік "Сума: " вивести_у_потік c вивести_у_потік "\n";
	вивести вивести_у_потік "привіт";
	повернути 0;
}

